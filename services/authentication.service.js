export default class AuthenticationService {
    constructor({

    }) {

    }
    
    // * POST /logout

    // * All APIs after login should be secured and only accessible to logged in user.
    authenticateUser() {
        // authenticate user
    }

}